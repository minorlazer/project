table
  thead
    tr
      th Name
      th Description
      th Price
      th Stock Quantity

  - @vault_name.each do |vault|
    tr
      td = link_to vault.name, product_path(:id => vault.id)
      td = vault.description
      td = number_to_currency(vault.price)
      td = vault.quantity

